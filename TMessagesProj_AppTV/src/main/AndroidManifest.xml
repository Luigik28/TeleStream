<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name="org.telegram.messenger.ApplicationLoaderImpl"
        android:label="@string/app_name"
        android:icon="@drawable/ic_telestream"
        android:banner="@drawable/tv_banner"
        tools:replace="android:name,android:label,android:icon,android:banner">

        <activity
            android:name="org.telegram.tv.TvLaunchActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|smallestScreenSize"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/TvTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="org.telegram.tv.login.TvLoginActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize"
            android:exported="false"
            android:theme="@style/TvTheme" />

        <activity
            android:name="org.telegram.tv.TvMainActivity"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize"
            android:exported="false"
            android:theme="@style/TvTheme" />

    </application>

</manifest>
